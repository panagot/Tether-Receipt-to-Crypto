# true = no model download; API returns fake OCR + totals (NOT from your image). false = real QVAC OCR+LLM.
USE_MOCK_AI=true
PORT=3847
SOLANA_RPC_URL=https://api.devnet.solana.com
# Optional when using a private RPC host (for explorer links + UI label)
# SOLANA_CLUSTER=devnet
USDT_MINT=
WALLET_SEED=
